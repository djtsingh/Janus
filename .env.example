# Copy to .env and fill values. Do NOT commit real secrets.

# Required - generate with: openssl rand -hex 32
JANUS_JWT_SECRET=

# Optional - defaults to redis:6379 in Docker
REDIS_ADDR=redis:6379

# Build metadata (set by CI/CD)
BUILD_VERSION=1.0.0
BUILD_DATE=2026-03-25
